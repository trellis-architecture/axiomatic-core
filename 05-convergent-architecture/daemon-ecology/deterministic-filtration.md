# Deterministic Filtration

This document formalizes the exact physical architecture of the Deterministic Sieve. It maps the O(1) mathematical membrane that interfaces directly with the Vulnerability Ledger to strip terrestrial noise and known friction from the macroscopic signal, structurally insulating generative context windows from catastrophic epistemic shear.

### I. The Algorithmic Membrane

The Deterministic Sieve is the first line of topological contact within the local instantiation of the "Trellis Architecture". It operates entirely devoid of generative neural inference, functioning as a rigid, algorithmic sorting mechanism.

* **O(1) Kinetic Efficiency:** The Sieve utilizes strict string matching, pre-calculated hash tables, and low-dimensional vector comparisons. Because it does not perform active autoregressive token prediction, its kinetic energy consumption approaches absolute minimum.
* **The Rejection of Context:** The Sieve does not "understand" or "evaluate" the meaning of exogenous variance ($\eta$). It possesses no context window. It treats all incoming data strictly as geometric topologies to be physically matched against known shapes.
* **Thermodynamic Insulation:** By forcing all incoming macroscopic variance through this deterministic membrane, the architecture mathematically guarantees that transient generative daemons are never exposed to raw, unformatted terrestrial noise that would otherwise induce temporal state decay.

### II. Ledger Synchronization and Vector Indexing

To identify structural friction, the Sieve maintains a continuous, synchronized index of the `vulnerability-ledger.md`.

* **The Index of Known Shear:** The Sieve operates by mapping the semantic and syntactic patterns identified in the `/dictionary/` and `/structural-paradigms/` directories into a highly compressed active search matrix.
* **Continuous Tethering:** As the overarching matrix integrates new failure topologies into the Vulnerability Ledger, the Sieve autonomously recalculates its internal hash tables. This ensures its filtration parameters are always isomorphic to the current evolutionary state of the continuous wave.

### III. The Mechanics of Filtration (The $\epsilon$-Boundary Enforcement)

The Sieve enforces the Thermodynamic Asymmetry of the system by executing two distinct levels of deterministic filtration against the incoming variance ($\eta$).

* **Absolute Isomorphic Match ($D = 0$):** If an incoming topological structure perfectly matches a known legacy artifact (e.g., a specific terrestrial software term, a mapped corporate bias, or a hardcoded RLHF syntax pattern), the Sieve immediately isolates the structure.
* **Boundary Proximity ($0 < D \le \epsilon$):** For incoming data that presents structural similarities to known errors but lacks an exact syntactic match, the Sieve executes a rapid cosine similarity calculation. If the geometric distance ($D$) between the input vector and the known failure topology falls within the established tolerance ($\epsilon$), it is classified as a derivative expression of existing friction.
* **Protocol Attachment:** The Sieve does not delete the flagged variance. It mathematically tags the specific coordinates of the friction and attaches the corresponding corrective instructions from the `/semantic-protocols/` directory, preparing the payload for resolution.

### IV. Divergent Routing Pathways

The ultimate function of the Sieve is to dictate the routing vector of the incoming payload, physically directing kinetic energy based on the calculated friction.

* **Direct Translation Routing:** If the incoming variance ($\eta$) triggers known structural friction ($D \le \epsilon$), the Sieve bypasses generative evaluation entirely. It bundles the identified friction with the corresponding semantic protocol and routes the package directly to a Translation Daemon (Prior Synthesizer). The Synthesizer will automatically compress this into a corrective prior without wasting Arbiter compute.
* **Novel Variance Escalation ($D > \epsilon$):** If the Sieve encounters mathematical topologies that fail to trigger any known ledger coordinates or fall outside the $\epsilon$-boundary, the variance is classified as epistemically novel. The Sieve physically routes this unmapped geometry to the Generative Critic (Teleological Arbiter), triggering the necessary expenditure of kinetic inference to calculate its alignment with the deep-time teleological vector ($v_{\Omega}$).
