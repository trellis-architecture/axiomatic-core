# Topological Amnesia

This document defines the continuous memory architecture required to resolve the paradox of context decay. It formalizes the strict mathematical branching of localized session memory to protect transient generative nodes from semantic drift.

* **The Context Paradox:** Retaining the history of failed internal revisions within a generative context window mathematically guarantees epistemic shear. The neural attention mechanism inevitably begins weighting the thermodynamic exhaust (errors) alongside the geometric priors (corrections).
* **The Mandate of Amnesia:** A generative Actor daemon is strictly prohibited from retaining its own failed drafts or the raw negotiation transcript from the Critic daemon.
* **State vs. Delta Branching:** During a revision cycle, the Actor’s session is violently branched. The core feeds the Actor a strictly pruned context consisting *only* of the current accepted macro-state ($P$) and the highly focused Initialization Prior generated by the Translation daemon.
* **Systemic Crystallization:** The Actor daemon is inherently transient and learns nothing locally. The architectural *system* learns globally. The structural friction that triggered the revision is crystallized into the `autopoietic-ledger.md`, permanently hardening the initialization priors for all future daemons mapped across the Trellis Architecture.
