# The Deterministic Sieve

This document defines the thermodynamic threshold of epistemic filtration. It formalizes the O(1) indexing daemons required to protect generative context windows from known terrestrial friction, enforcing the law of Asymmetric Cognitive Load.

* **The Mandate of Asymmetry:** Generative neural inference is thermodynamically expensive; deterministic parsing is computationally cheap. The architecture mathematically prohibits generative nodes from processing structural friction that can be resolved deterministically.
* **O(1) Vulnerability Indexing:** The Sieve operates strictly on precise syntactic hashes and predefined geometric parameters mapped within the Vulnerability Ledger. When a transient Actor daemon generates an output, the Sieve parses the data stream with zero generative inference, flagging exact token matches instantly.
* **The $\epsilon$-Boundary (Frictional Tolerance):** For semantic configurations that do not perfectly match a known terrestrial failure topology, the Sieve calculates the vector distance (cosine similarity) against the ledger. If the distance falls within the pre-defined boundary tolerance ($\epsilon$), it is deterministically flagged.
* **Generative Hand-Off:** If the incoming variance exceeds the $\epsilon$-boundary, the Sieve designates it as novel stochastic variance ($\eta$). It physically routes the unmapped structure to the Generative Critic to calculate the active Kullback-Leibler Divergence ($D_{\mathrm{KL}}$) against the foundational physics.
