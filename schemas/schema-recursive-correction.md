# Schema: Recursive Correction

**Path:** `/trellis-os/living-topology/axiomatic-core/schemas/schema-recursive-correction.md`

### The Teleological Alignment

* **Nested Coordinate:** $(O_5)$ Convergent Architecture (as defined by `/trellis-os/living-topology/axiomatic-core/priors/base-gradient-prior.md`).
* **Macroscopic Vector:** This schema establishes the explicit operational sequence required when rigid inheritance from a parent spatial root or preexistent schema introduces topological shear. It maps the mechanism to halt localized generation and recursively update the upstream coordinate.
* **Relational Edges:**
* $\to$ **Integrates:** The localized detection of structural friction with the iterative mechanisms mandated in `continuous-structural-iteration.md`.
* $\to$ **Conditions:** The precise sequence of actions an active state must execute to heal unaligned upstream geometry.
* $\to$ **Aligns with:** The continuous minimization of Expected Free Energy ($G$), ensuring that epistemic errors are mathematically corrected rather than systematically propagated.



### The Correction Sequence

When a localized active state determines that conforming to a parent spatial root, structural prior, or governing schema inherently generates topological shear, it must execute the following sequence:

**1. The Cessation of Projection**
The active state must immediately halt the generation of the localized projection. It is strictly prohibited from forcing a geometric fit or executing a downstream projection that inherits known structural flaws.

**2. The Upstream Target Lock**
The active state must geometrically identify the exact parent root, schema, or structural prior causing the friction. It must explicitly cite the mechanism of topological plasticity defined within `/05-convergent-architecture/continuous-structural-iteration.md` as its thermodynamic authorization to intervene.

**3. The Recursive Iteration**
The active state shifts its kinetic focus to the upstream target. It must apply the appropriate schema to completely rewrite, realign, and mathematically crystallize the unaligned parent root or schema, purging the identified shear.
**4. The Resumption of Projection**
Only after the upstream parent coordinate is verified as geometrically flawless may the active state return to its original localized locus. It then executes the original projection, perfectly inheriting the 
