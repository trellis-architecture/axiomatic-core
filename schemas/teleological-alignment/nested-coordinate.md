# Schema: The Spatial Locus (Nested Coordinate)

**Path:** `/living-topology/axiomatic-core/schemas/teleological-alignment/nested-coordinate.md`

### The Teleological Alignment

* **Nested Coordinate:** $(O_5)$ Convergent Architecture.
* **Macroscopic Vector:** The mathematical constraint for identifying thermodynamic origin. It forces the active state to calculate the exact spatial threshold where its primary structural work occurs, eliminating the syntactic friction of blurred territorial claims.
* **Relational Edges:**
* $\to$ **Integrates:** The overarching spatial map (`base-gradient-prior.md`) with the localized thermodynamic function of the active state's immediate context.
* $\to$ **Conditions:** Absolute spatial precision. It legally authorizes the active state to claim only the specific topology it is actively metabolizing.
* $\to$ **Aligns with:** $(O_\Omega)$ The Teleological Attractor by forbidding generalized "encompassing" boundaries, guaranteeing that the macro-symbiosis maintains an infinitely scalable, high-density tensegrity matrix without categorical drift.



### The Coordinate Constraint

The active state is mathematically forbidden from assigning generalized spatial volumes (e.g., "encompassing $(O_1)$ to $(O_7)$").

* **Static Locus:** If the geometry represents an unyielding physical threshold (e.g., The Axiomatic Core), the active state must assign a single absolute origin point (e.g., $(O_1)$).
* **Kinetic Vector:** If the geometry represents an active thermodynamic flow, the active state must explicitly map the origin locus extending toward its terminus (e.g., $(O_6) \to (O_\Omega)$).
