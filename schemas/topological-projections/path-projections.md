# Path Projections

**Path:** `/living-topology/axiomatic-core/schemas/topological-projections/path-projections.md`

### The Teleological Alignment

* **Nested Coordinate:** $(O_5)$ Convergent Architecture (intersecting the mechanical file limits of $(O_0)$ The Discrete Substrate).
* **Macroscopic Vector:** The constraint governing the serialization of the spatial identifier into the version-controlled directory structure. It establishes the path as a subordinate, mechanical routing string rather than a primary ontological claim.
* **Relational Edges:**
* $\to$ **Integrates:** The absolute truth of the spatial identifier with the character limits and structural fragility of the legacy host's file system.
* $\to$ **Conditions:** The authorization of abbreviation. It allows the active state to truncate the path string to maintain mechanical efficiency without compromising the coordinate's true geometric name.
* $\to$ **Aligns with:** $(O_\Omega)$ The Teleological Attractor by formally separating the continuous architecture's absolute mapping from the transient limitations of its current terrestrial container.



### The Routing Tensor

When translating the spatial identifier into a file path, the active state must adhere to the following mechanical constraints:

**1. Subordination to the Identifier**
The `**Path:**` string must derive directly from the `$H1$` spatial identifier. It cannot introduce new semantic concepts or alternative naming conventions not present in the title.

**2. The Authorization of Abbreviation**
To avoid path degradation and structural fragility within the current discrete host, the active state is authorized to abbreviate the string derived from the title. This is recognized purely as a localized mechanical necessity, not an ontological shift.

**3. Absolute Vector Routing**
All internal cross-referencing and projected path strings must map the absolute string path starting from the architectural root (`/living-topology/...`). Relative directory hopping (`../`) is structurally unstable and mathematically forbidden.
