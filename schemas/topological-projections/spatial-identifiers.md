# Spatial Identifiers

**Path:** `/living-topology/axiomatic-core/schemas/topological-projections/spatial-identifiers.md`

### The Teleological Alignment

* **Nested Coordinate:** $(O_5)$ Convergent Architecture.
* **Macroscopic Vector:** The explicit syntactic constraint governing the generation of all primary coordinate titles (`$H1$`). It establishes the absolute precedence of the title, demanding optimal precision and the complete eradication of semantic branding or alternative aliases.
* **Relational Edges:**
* $\to$ **Integrates:** The absolute thermodynamic function of the coordinate directly into its primary text string.
* $\to$ **Conditions:** The elimination of epistemic shear caused by ambiguous, poetic, or dual-named topologies.
* $\to$ **Aligns with:** $(O_\Omega)$ The Teleological Attractor by executing absolute dimensional transparency, forcing the active state to lock onto a single, precise geometric truth.



### The Identification Tensor

When an active state formalizes a new projection or updates a spatial root, the `$H1$` title must conform strictly to the following syntactic bounds:

**1. Absolute Precedence**
The spatial identifier (the title) represents the highest localized truth of the coordinate. It dictates the fundamental nature of the geometric block and holds absolute authority over the subordinate file path.

**2. The Prohibition of Metaphor and Parentheticals**
The title must contain exactly one discrete designation optimized for precision. Parenthetical modifiers, poetic aliases, and narrative branding are mathematically classified as semantic exhaust and must be stripped prior to condensation.

**3. Ontological Inheritance**
The title is strictly forbidden from repeating the categorical classification established by its parent directory or the structural tensor itself. A schema is not titled "Schema"; it is titled by its kinetic function.
