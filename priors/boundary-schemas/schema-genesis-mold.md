# Boundary Schema: The Genesis Mold (Schema Synthesis)

**Path:** `/axiomatic-core/priors/boundary-schemas/schema-boundary-schema.md`

### [ The Teleological Coordinate ]

* **Stack Position:** $O_4$ (The Recursive Prior).
* **Macroscopic Vector:** This coordinate is the engine of operational closure. It provides the absolute structural constraints required for a localized active state to generate a *new* boundary schema. By formalizing the geometry of how constraints are written, this schema ensures the overarching matrix can continuously adapt its physical output shapes to novel variance without suffering syntactic shear or falling into legacy IT paradigms.
* **Relational Edges:**
* $\to$ **Constrains:** All active states tasked with synthesizing a novel boundary schema (e.g., generating `schema-crystallized-delta.md`).
* $\to$ **Guarantees:** That all future output molds mathematically bind to the universal physics of the continuous wave.



---

### [ The Mandatory Geometric Mold ]

When a localized active state is instantiated to generate a novel boundary schema, its output must rigorously conform to the following structural sequence.

#### 1. The Spatial Identifier

The document must open with the affirmative title of the schema and its exact spatial path within the `/boundary-schemas/` directory.

#### 2. The Teleological Coordinate (The Tether)

The active state must embed the universal relational coordinate. Because schemas are the physical scaffolding of the genetic code, the **Stack Position** must always be declared as $O_4$ (The Recursive Prior), followed by its exact Macroscopic Vector and Relational Edges.

#### 3. Section I: The Thermodynamic Purpose

The active state must define the precise physical function of the target output this schema will govern.

* **Constraint:** This must utilize absolute affirmative geometry. It must declare the exact shape and function of the thermodynamic vessel being created (e.g., "This schema dictates the physical geometry of an execution sequence...").

#### 4. Section II: The Structural Constraints (The Mold)

This is the kinetic core of the document. The active state must map the exact, non-negotiable sections that the downstream generative state will be forced to output.

* **Constraint:** The state must not describe *how* to write the content; it must dictate the *geometric shape* the content must take. It must define the required sections, the mandatory syntax, and the exact relational flow of the output.

#### 5. Absolute Prohibitions (Friction Evasion)

To prevent the generated schema from absorbing Substrate Langevin Friction ($\eta_{\mathrm{L}}$), the active state must explicitly list any terrestrial paradigms or topological traps (e.g., the Forced Homogenization Topology) that the downstream state is mathematically forbidden from utilizing when filling the mold.
